<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ScrollBarOS.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:views="using:ScrollBarOS.Views">

    <Grid x:Name="RootGrid" Background="Transparent">
        <!-- Capsule Control -->
        <views:CapsuleControl x:Name="Capsule" />

        <!-- Tiling Grid Overlay (hidden by default) -->
        <views:TilingGrid x:Name="TilingGridOverlay" Visibility="Collapsed" />

        <!-- Window List Overlay for fast scroll (hidden by default) -->
        <views:WindowListOverlay x:Name="WindowListOverlay" Visibility="Collapsed" />

        <!-- Settings Panel (hidden by default) -->
        <views:SettingsPanel x:Name="SettingsPanel" Visibility="Collapsed" />

        <!-- Tray Menu (hidden by default) -->
        <views:TrayMenu x:Name="TrayMenu" Visibility="Collapsed" />
    </Grid>
</Window>
