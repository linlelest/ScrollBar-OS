<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ScrollBarOS.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">

    <Border x:Name="_rootBorder" Background="#CC1E1E2E" CornerRadius="32" Padding="8">
        <Grid x:Name="_rootGrid">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="auto" />
            </Grid.RowDefinitions>

            <!-- Main scrolling area with app icons and optional window list -->
            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Hidden">
                <Grid>
                    <StackPanel x:Name="_appIconsPanel" HorizontalAlignment="Center" Margin="0,12,0,8" />
                    <StackPanel x:Name="_windowListPanel" HorizontalAlignment="Stretch" Margin="4,12,4,8" Visibility="Collapsed" />
                </Grid>
            </ScrollViewer>

            <!-- Bottom controls -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Bottom">
                <TextBlock x:Name="_dateTimeText" FontSize="9" Foreground="#AAFFFFFF" HorizontalAlignment="Center" Margin="0,2,0,2" />

                <StackPanel HorizontalAlignment="Center">
                    <TextBlock x:Name="_cpuText" Text="CPU: --" FontSize="9" Foreground="#AAFFFFFF" Margin="4,1" />
                    <TextBlock x:Name="_memText" Text="RAM: --" FontSize="9" Foreground="#AAFFFFFF" Margin="4,1" />
                    <TextBlock x:Name="_diskText" Text="Disk: --" FontSize="9" Foreground="#AAFFFFFF" Margin="4,1" />
                    <TextBlock x:Name="_netText" Text="Net: --" FontSize="9" Foreground="#AAFFFFFF" Margin="4,1" />
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button x:Name="TilingButton" Click="TilingButton_Click" Background="Transparent" BorderThickness="0" Padding="4" Margin="2" ToolTipService.ToolTip="Tile Windows">
                        <FontIcon Glyph="&#xE740;" FontSize="12" Foreground="#AAFFFFFF"/>
                    </Button>
                    <Button x:Name="SettingsButton" Click="SettingsButton_Click" Background="Transparent" BorderThickness="0" Padding="4" Margin="2">
                        <FontIcon Glyph="&#xE713;" FontSize="14" Foreground="#CCFFFFFF"/>
                    </Button>
                </StackPanel>
                <Button x:Name="CloseButton" Click="CloseButton_Click" Background="Transparent" HorizontalAlignment="Center">
                    <FontIcon Glyph="&#xE7E8;" FontSize="20" Foreground="#CCFFFFFF"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
