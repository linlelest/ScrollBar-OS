<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ScrollBarOS.TilingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Title="Window Tiling">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="20,20,20,14">
            <TextBlock Text="Window Tiling" FontSize="18" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
            <TextBlock x:Name="CountText" Text="" FontSize="12" Foreground="#AAFFFFFF" VerticalAlignment="Center" Margin="12,0,0,0"/>
        </StackPanel>

        <!-- Grid Preview Area -->
        <ScrollViewer Grid.Row="1" Margin="20,0,20,20">
            <Border x:Name="GridPreviewBorder" Background="#1A1A28" CornerRadius="12" Padding="20">
                <ItemsRepeater x:Name="ItemsRepeater" 
                               ItemsSource="{Binding SelectedWindows}"
                               Layout="{StaticResource GridLayout}">
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#2A2A3A" CornerRadius="8" Margin="5">
                                <Grid>
                                    <Image Source="{Binding Icon}" Width="32" Height="32" Margin="8"/>
                                    <TextBlock Text="{Binding Title}" Foreground="White" FontSize="12" 
                                               TextWrapping="Wrap" Margin="48,8,8,8" MaxLines="2"/>
                                    <Button Content="✕" Background="Transparent" BorderThickness="0"
                                            Width="20" Height="20" HorizontalAlignment="Right" VerticalAlignment="Top"
                                            Margin="4" FontSize="12" Click="DeleteButton_Click" Tag="{Binding}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
            </Border>
        </ScrollViewer>

        <!-- Footer Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,20,20,20">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button Content="Tile Windows" Width="120" Click="TileButton_Click"/>
        </StackPanel>
    </Grid>
</Window>