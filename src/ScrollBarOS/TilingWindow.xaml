<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ScrollBarOS.TilingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">

    <Border Background="#F51A1A28" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,14">
                <TextBlock Text="Window Tiling" FontSize="18" FontWeight="Bold" Foreground="#FFFFFFFF" VerticalAlignment="Center" />
                <TextBlock x:Name="CountText" Text="" FontSize="12" Foreground="#88FFFFFF" VerticalAlignment="Center" Margin="12,0,0,0" />
            </StackPanel>

            <!-- Grid preview area -->
            <ScrollViewer Grid.Row="1" x:Name="ScrollViewer" VerticalScrollBarVisibility="Auto" AllowDrop="True">
                <ItemsRepeater x:Name="ItemsRepeater">
                    <ItemsRepeater.Layout>
                        <UniformGridLayout MinItemWidth="140" MinItemHeight="100" MinColumnSpacing="10" MinRowSpacing="10" ItemsStretch="Fill" />
                    </ItemsRepeater.Layout>
                </ItemsRepeater>
            </ScrollViewer>

            <!-- Bottom bar -->
            <Grid Grid.Row="2" Margin="0,14,0,0">
                <TextBlock Text="Drag app icons here, then confirm to tile." FontSize="11" Foreground="#77FFFFFF" VerticalAlignment="Center" />
                <Button x:Name="ConfirmButton" Content="Confirm Tiling" HorizontalAlignment="Right" Background="#FF4CC2FF" Foreground="#FFFFFFFF" Padding="18,8,18,8" CornerRadius="6" FontWeight="SemiBold" />
            </Grid>
        </Grid>
    </Border>
</Window>