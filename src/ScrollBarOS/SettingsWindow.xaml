<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ScrollBarOS.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Settings" FontSize="22" FontWeight="Bold" Margin="0,0,0,16"/>

            <!-- Capsule Position -->
            <TextBlock Text="Capsule Position" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <ComboBox x:Name="PositionComboBox" HorizontalAlignment="Stretch" Margin="0,0,0,16">
                <ComboBoxItem Content="Left" />
                <ComboBoxItem Content="Right" />
            </ComboBox>

            <!-- Background Material -->
            <TextBlock Text="Background Material" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <ComboBox x:Name="MaterialComboBox" HorizontalAlignment="Stretch" Margin="0,0,0,16">
                <ComboBoxItem Content="Solid" />
                <ComboBoxItem Content="Acrylic" />
                <ComboBoxItem Content="Mica" />
            </ComboBox>

            <!-- Opacity -->
            <TextBlock x:Name="OpacityLabel" Text="Opacity: 100%" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <Slider x:Name="OpacitySlider" Minimum="20" Maximum="100" Value="100" StepFrequency="5" Margin="0,0,0,16"/>

            <!-- Capsule Width -->
            <TextBlock x:Name="WidthLabel" Text="Capsule Width: 64px" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <Slider x:Name="WidthSlider" Minimum="48" Maximum="96" Value="64" StepFrequency="4" Margin="0,0,0,16"/>

            <!-- Icon Size -->
            <TextBlock x:Name="IconSizeLabel" Text="Icon Size: 32px" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <Slider x:Name="IconSizeSlider" Minimum="24" Maximum="48" Value="32" StepFrequency="4" Margin="0,0,0,16"/>

            <!-- Widgets -->
            <TextBlock Text="Widgets" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <ToggleSwitch x:Name="HardwareWidgetToggle" Header="Hardware Monitor" Margin="0,0,0,8"/>
            <ToggleSwitch x:Name="DateTimeWidgetToggle" Header="Date &amp; Time" Margin="0,0,0,16"/>

            <!-- System -->
            <TextBlock Text="System" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <ToggleSwitch x:Name="HideTaskbarToggle" Header="Hide Taskbar" Margin="0,0,0,8"/>
            <ToggleSwitch x:Name="StartupToggle" Header="Start with Windows" Margin="0,0,0,16"/>

            <!-- Version -->
            <TextBlock Text="ScrollBar OS v1.0.0" FontSize="11" Opacity="0.5" Margin="0,16,0,0" />
        </StackPanel>
    </ScrollViewer>
</Window>