<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="ScrollBarOS.SettingsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    Title="ScrollBar OS - Settings">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <TextBlock Text="Settings" FontSize="22" FontWeight="Bold" Margin="0,0,0,16"/>
            
            <!-- Capsule Position -->
            <TextBlock Text="Capsule Position" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <ComboBox x:Name="PositionCombo" Margin="0,0,0,16">
                <ComboBoxItem Content="Left"/>
                <ComboBoxItem Content="Right"/>
            </ComboBox>

            <!-- Background Material -->
            <TextBlock Text="Background Material" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <ComboBox x:Name="MaterialCombo" Margin="0,0,0,16">
                <ComboBoxItem Content="Solid"/>
                <ComboBoxItem Content="Acrylic"/>
                <ComboBoxItem Content="Mica"/>
            </ComboBox>

            <!-- Background Opacity -->
            <TextBlock x:Name="OpacityText" Text="Opacity: 80%" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <Slider x:Name="OpacitySlider" Minimum="20" Maximum="100" TickFrequency="5" IsSnapToTickEnabled="True" Margin="0,0,0,16"/>

            <!-- Capsule Width -->
            <TextBlock x:Name="WidthText" Text="Capsule Width: 64px" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <Slider x:Name="WidthSlider" Minimum="48" Maximum="96" TickFrequency="4" IsSnapToTickEnabled="True" Margin="0,0,0,16"/>

            <!-- Icon Size -->
            <TextBlock x:Name="IconText" Text="Icon Size: 32px" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <Slider x:Name="IconSlider" Minimum="24" Maximum="48" TickFrequency="4" IsSnapToTickEnabled="True" Margin="0,0,0,16"/>

            <!-- Widgets -->
            <TextBlock Text="Widgets" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <ToggleSwitch x:Name="HardwareToggle" Header="Hardware Monitor" Margin="0,0,0,8"/>
            <ToggleSwitch x:Name="DateTimeToggle" Header="Date & Time" Margin="0,0,0,16"/>

            <!-- System -->
            <TextBlock Text="System" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
            <ToggleSwitch x:Name="TaskbarToggle" Header="Hide Taskbar" Margin="0,0,0,8"/>
            <ToggleSwitch x:Name="ScrollToggle" Header="Start with Windows" Margin="0,0,0,16"/>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="CancelButton_Click"/>
                <Button Content="Apply" Width="80" Click="ApplyButton_Click"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Window>